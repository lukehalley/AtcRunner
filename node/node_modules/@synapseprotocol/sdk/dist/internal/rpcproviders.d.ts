import type { Provider } from "@ethersproject/providers";
/**
 * @param chainId chain id of the network for which to return a provider
 */
export declare function rpcProviderForChain(chainId: number): Provider;
export interface RPCEndpointsConfig {
    [chainId: number]: {
        endpoint: string;
        batchInterval?: number;
    };
}
export declare function configureRPCEndpoints(config: RPCEndpointsConfig): void;
