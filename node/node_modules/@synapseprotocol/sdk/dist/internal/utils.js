import _ from "lodash";
import { Tokens } from "../tokens.js";
function tokenReducer(check) {
    const ret = _.find(Tokens.AllTokens, (t => check.isEqual(t)));
    return !ret ? undefined : ret;
}
export const tokenSwitch = (check) => tokenReducer(check);
