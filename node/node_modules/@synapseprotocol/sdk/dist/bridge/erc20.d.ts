import { Signer } from "@ethersproject/abstract-signer";
import type { TransactionResponse } from "@ethersproject/abstract-provider";
import { BigNumber, type BigNumberish } from "@ethersproject/bignumber";
import type { PopulatedTransaction } from "@ethersproject/contracts";
export declare const MAX_APPROVAL_AMOUNT: BigNumber;
export declare namespace ERC20 {
    type ApproveArgs = {
        spender: string;
        amount?: BigNumberish;
    };
    type ERC20TokenParams = {
        tokenAddress: string;
        chainId: number;
    };
    function approve(approveArgs: ApproveArgs, tokenParams: ERC20TokenParams, signer: Signer): Promise<TransactionResponse>;
    function buildApproveTransaction(approveArgs: ApproveArgs, tokenParams: ERC20TokenParams): Promise<PopulatedTransaction>;
    function balanceOf(address: string, tokenParams: ERC20TokenParams): Promise<BigNumber>;
    function allowanceOf(owner: string, spender: string, tokenParams: ERC20TokenParams): Promise<BigNumber>;
}
