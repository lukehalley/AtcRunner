import _ from "lodash";
import { Tokens } from "../tokens.js";
function tokenReducer(check) {
    var ret = _.find(Tokens.AllTokens, (function (t) { return check.isEqual(t); }));
    return !ret ? undefined : ret;
}
export var tokenSwitch = function (check) { return tokenReducer(check); };
